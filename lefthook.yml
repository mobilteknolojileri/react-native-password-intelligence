pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,mjs,ts,jsx,tsx}"
      run: npx eslint {staged_files}
    types:
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit -p tsconfig.json
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
